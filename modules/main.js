//This file loads all the modules for the terminal commands

//global variables
require(['globals']);

//the terminal application (hopefully I will find a way to break this down into smaller files eventually)
require(['terminal']);