//all the functions are loaded in this script
//if you want to customize, this is the place to remove or add commands
//just be sure to add or remove it's corresponding section in the interpreter.js

require(['commands/queueTrack']);
require(['commands/queueDisplay']);
require(['commands/playTrack']);
require(['commands/playNextTrack']);
require(['commands/timedComment']);
require(['commands/displayTimedComments']);
require(['commands/playOrQueue']);
require(['commands/stopTrack']);
require(['commands/pauseTrack']);
require(['commands/tracks']);
require(['commands/listTracks']);
require(['commands/listTracksTracks']);
require(['commands/listTracksStream']);
require(['commands/moreTracksTracks']);
require(['commands/moreTracksStream']);
require(['commands/formatTracks']);
require(['commands/getNextHref']);
require(['commands/links']);
require(['commands/turnLinksOn']);
require(['commands/turnLinksOff']);
require(['commands/help']);
require(['commands/soundcloud']);
require(['commands/facebook']);
require(['commands/about']);
require(['commands/followVerify']);
require(['commands/follow']);
require(['commands/repeat']);
require(['commands/comment']);
require(['commands/api']);
require(['commands/artist']);
require(['commands/login']);
require(['commands/logout']);
require(['commands/whoami']);
require(['commands/like']);